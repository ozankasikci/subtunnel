{
  "name": "subtunnel",
  "private": true,
  "scripts": {
    "dev:web": "pnpm --filter @subtunnel/web dev",
    "dev:dashboard": "pnpm --filter @subtunnel/dashboard dev",
    "build": "pnpm -r build"
  }
}
